fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
pydantic>=2.0.0
anthropic>=0.40.0
langgraph>=0.2.0
python-dateutil>=2.8.0
PyPDF2>=3.0.0
openpyxl>=3.1.0
# OCR for scanned/image documents is handled by Claude's native PDF + vision
# support (see agents/ocr_agent.py). We intentionally do NOT use tesseract /
# pdf2image / Pillow here because those require system binaries (tesseract,
# poppler) that are not available in the serverless runtime.
